version: '3.8'

# Using Neo4j Aura (cloud-hosted) - local Neo4j container commented out
# Uncomment below if you need to run Neo4j locally for development

services: {}
  # neo4j:
  #   image: neo4j:5.15.0
  #   container_name: epihelix-neo4j
  #   ports:
  #     - "7474:7474"  # HTTP (Neo4j Browser)
  #     - "7687:7687"  # Bolt protocol
  #   environment:
  #     - NEO4J_AUTH=neo4j/epihelix123  # Username/Password
  #     - NEO4J_PLUGINS=["apoc"]        # APOC plugin for advanced procedures
  #     - NEO4J_dbms_memory_heap_max__size=2G
  #     - NEO4J_dbms_memory_pagecache_size=1G
  #     - NEO4J_dbms_security_procedures_unrestricted=apoc.*
  #     - NEO4J_dbms_security_procedures_allowlist=apoc.*
  #   volumes:
  #     - neo4j_data:/data
  #     - neo4j_logs:/logs
  #     - neo4j_import:/var/lib/neo4j/import
  #     - neo4j_plugins:/plugins
  #   networks:
  #     - epihelix-network
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "cypher-shell", "-u", "neo4j", "-p", "epihelix123", "RETURN 1"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

# Volumes and networks commented out since using Neo4j Aura
# volumes:
#   neo4j_data:
#     driver: local
#   neo4j_logs:
#     driver: local
#   neo4j_import:
#     driver: local
#   neo4j_plugins:
#     driver: local

# networks:
#   epihelix-network:
#     driver: bridge
